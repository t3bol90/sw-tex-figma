# Plugin icon

- `plugin-icon.svg` is the editable vector source.
- `plugin-icon-512.png` is the 512 × 512 publishing asset.

Figma plugin manifests do not reference a marketplace icon. Upload the PNG when publishing the plugin in Figma Community.
